apiVersion: v1
kind: Service
metadata:
  name: {{ include "ds0.fullname" . }}
  labels:
    {{- include "ds0.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ds0.selectorLabels" . | nindent 4 }}
